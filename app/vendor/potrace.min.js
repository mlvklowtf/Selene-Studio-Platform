/* Local stub of potrace.js (vectorization). Replace with the real library when available. */
(function(global){
  var Potrace = function(){};
  Potrace.prototype = {
    loadImageFromDataURL: function(){ console.warn('potrace.stub: loadImageFromDataURL'); },
    process: function(){ console.warn('potrace.stub: process'); }
  };
  global.Potrace = Potrace;
})(this);
