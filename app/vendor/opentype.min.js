/* Local stub of opentype.js to avoid external CDN usage.
   Replace with the real library if needed. */
(function(global){
  // Minimal stub to prevent runtime errors when code calls opentype functions
  var opentype = {
    load: function(url, callback) {
      console.warn('opentype.load called on local stub for', url);
      callback(new Error('opentype.js stub: no implementation'), null);
    }
  };
  if (typeof module !== 'undefined' && module.exports) module.exports = opentype;
  global.opentype = opentype;
})(this);
